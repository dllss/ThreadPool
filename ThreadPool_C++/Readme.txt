1、这是一个利用C语言实现线程池的程序，实现线程池，并没有资源的损耗
2、实现了线程池的可扩展
3、相比于之前的线程池程序，该程序的优点在于add_task的过程中也进行了lock处理，保证了signal并不会丢失
4、该程序的thread_routine并没有一开始就lock并wait而只是lock并判断任务链表是否为空，为空的话才进行wait并且空闲线程数加1，这样在add_task时检测到空闲线程数不为0，则发送signal请求，保证wait能真确地响应。